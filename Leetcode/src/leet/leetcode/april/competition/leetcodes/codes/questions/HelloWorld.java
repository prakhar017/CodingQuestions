package leetcode.april.competition.leetcodes.codes.leetcode.oldcodes;

import java.util.Arrays;

public class HelloWorld {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		System.out.println("hey");
		
		
/*		STACK:
			s.empty( );
			s.push(c )
			s.pop( )


		MAPS:
			map.containsKey( );
			map.get( );
			map.put( );


		RETURN :
			return new int[ ] { a, b ,c };
			when we use this  :  return null;    and only return ;

		STRING:
			String s = new StringBuilder().append(Math.abs(x)).reverse().toString();
			 x= x<0 ? Integer.parseInt(s) * -1 : Integer.parseInt(s);


			String val = String.valueOf(x);

			char[ ] ch = s.toCharArray( );


			string1.indexOf(string2);
			s.substring(0, l);
			s.isEmpty();   or s.length()==0
			s.equals(s1)
		    s.split(" ");
		    s.trim();

		    public int lengthOfLastWord(String s) {
	        String[] arr = s.split(" ");
	        if (arr.length == 0) return 0;
	        else return arr[arr.length - 1].length();
	    }

*/
		
		
		
		//Character.isLetterOrDigit(s.charAt(i))
		//Character.toLowerCase(s.charAt(i))
		

/*
		MATH:
			Math.abs ( )
			Math.min( s1.length() , s2.length())
			if(rev >Integer.MAX_VALUE || rev < Integer.MIN_VALUE)
*/


		//Arrays.sort(c);


/*
		LIST:
		list.sort(null);
		list.size();
        list.get(index)
		list.add(value)
*/


	}

}
